# 算法设计与分析 — 期末汇报演讲稿

## 题目：【动态规划 vs 贪心】电商促销场景下的最优商品预算组合（背包问题变种）

---

## 一、开场（1 分钟）

各位老师、同学好，我是 [你的名字]，我们小组的选题是**电商促销场景下的最优商品预算组合**。

先问大家一个问题：双十一的时候，你手里有一张满减券，购物车里加了十几件商品，但预算有限——你会怎么选？是挑最喜欢的？还是挑最划算的？

这就是我们今天要解决的问题：**在用户预算和平台补贴的双重约束下，如何选出"用户最开心、平台也最赚钱"的商品组合。**

---

## 二、问题背景与建模（2 分钟）

### 2.1 实际背景

电商平台大促期间，有两个核心利益方：
- **用户**：手里有满减券或消费预算，想买到最满意的商品
- **平台**：有一笔补贴资金池，想在帮用户省钱的同时最大化自己的佣金收益

这两个目标是矛盾的——用户喜欢的商品不一定利润高，利润高的商品用户不一定喜欢。

### 2.2 问题建模

我们将这个问题形式化为一个**二维约束的 0-1 背包问题**：

- **背包容量有两个维度**：用户预算 W 和平台补贴 B
- **每件商品有两个成本**：用户原价（消耗用户预算）和平台补贴（消耗补贴池）
- **价值函数**：采用加权线性组合——**复合价值 = α × 用户喜爱度 + β × 商家佣金**

其中 α + β = 1，α 越大越偏用户满意度，β 越大越偏平台收益。用户可以通过滑块实时调节这个权重，适应不同的业务策略。

这个公式是多目标优化中的标准方法——**加权和法（Weighted Sum Method）**，在学术上是被广泛认可的。

---

## 三、算法设计（3 分钟）

我们设计了三种算法，各有不同的定位。

### 3.1 主推算法：空间优化 DP（分治回溯）

核心思想是**动态规划**。我们定义状态 `dp[w][b]` 表示用户预算为 w、平台补贴为 b 时能获得的最大复合价值。转移方程是：

```
dp[w][b] = max(dp[w][b], dp[w - price_i][b - subsidy_i] + score_i)
```

这是经典的 0-1 背包转移，但我们有两个约束维度，所以是一个**二维背包**。

**但我们没有用标准实现。** 标准 DP 需要一个 `keep[N][W][B]` 的三维数组来记录每一步的选择，空间复杂度是 O(N×W×B)。当商品数 N 增大时，这个数组会占用数百 MB 内存。

所以我们实现了**基于 Hirschberg 思想的分治回溯优化**：

1. 将商品二分：前半段和后半段
2. 前半段正向跑 DP，后半段反向跑 DP
3. 枚举分割点，找到前后半的最优预算分配
4. 递归处理左右两半

这样我们只需要 O(W×B) 的空间——**空间降低了 N 倍，结果与标准 DP 完全一致**。代价是时间多了约一倍，但在毫秒级差异下用户完全无感知。

### 3.2 对比算法：贪心算法

贪心的策略很简单：按**单位成本复合价值比**从高到低排序，依次选取能装下的商品。

- 时间复杂度：O(N·logN)，主要来自排序
- 空间复杂度：O(N)
- **优点**：极快，适合高并发场景
- **缺点**：不保证全局最优，可能错过更好的组合

### 3.3 验证工具：暴力回溯

暴力枚举所有 2^N 种商品组合，逐个检查约束，找到真正的全局最优解。

- 时间复杂度：O(2^N)，指数级
- **不参与推荐**，只用于小规模下验证 DP 的正确性

---

## 四、算法分析（2 分钟）

### 4.1 时间复杂度对比

| 算法 | 时间复杂度 | N=100 耗时 | N=200 耗时 |
|------|-----------|-----------|-----------|
| 优化 DP | O(N×W×B×logN) | ~0.005 秒 | ~0.012 秒 |
| 贪心 | O(N×logN) | ~0.0001 秒 | ~0.0002 秒 |
| 暴力 | O(2^N) | 不可行 | 不可行 |

贪心在时间上有绝对优势，但 DP 的耗时也在毫秒级，完全可接受。

### 4.2 空间复杂度对比

| 算法 | 空间复杂度 | N=100 内存 | N=200 内存 |
|------|-----------|-----------|-----------|
| 标准 DP | O(N×W×B) | ~80 MB | ~160 MB |
| **优化 DP** | **O(W×B)** | **~1.2 MB** | **~1.2 MB** |
| 贪心 | O(N) | ~0.01 MB | ~0.02 MB |

优化 DP 的空间与商品数无关——这正是我们选择它作为主推算法的原因。

### 4.3 结果质量对比

优化 DP 保证**数学最优解**，贪心是**近似解**。在我们的实验中，贪心通常落后 DP 约 3%~8%，具体取决于权重配置和商品分布。

### 4.4 正确性验证

我们用暴力回溯穷举在 N=5~15 的范围内逐一验证，优化 DP 与暴力穷举的结果**完全一致**，证明了实现的正确性。

---

## 五、系统演示（3 分钟）

我们用 Streamlit 构建了一个交互式网页系统，现在给大家演示。

**（打开网页 http://xxx.streamlit.app）**

### 5.1 左侧栏：全局配置

这里可以调节三个核心参数：
- 用户消费预算：模拟用户的满减券额度
- 平台补贴资金池：模拟平台的补贴预算
- 消费者体验权重 α：越往右拉，算法越偏用户满意度；越往左拉，越偏平台收益

### 5.2 左侧：购物车数据

默认有 6 件商品，包含智能手环、降噪耳机、机械键盘等。可以手动添加商品，也可以批量生成随机商品用于压测。

### 5.3 右侧：算法对比结果

上方展示了两种算法的得分对比，以及贪心落后 DP 的百分比。可以看到当前配置下贪心落后约 X%，说明在这个场景下贪心的质量还不错，但 DP 仍然更优。

切换两个 Tab 可以看到各自推荐了哪些商品、预算占用情况。

### 5.4 底部：性能曲线

点击运行基准测试，可以看到随着商品数量增加，优化 DP 的耗时逐渐上升，而贪心几乎平躺——这直观展示了两种算法时间复杂度的差异。

### 5.5 折叠面板

底部有两个折叠面板：
- **空间优化对比**：展示了标准 DP 和优化 DP 的内存差异，可以看到 N 增大时标准 DP 内存爆炸，而优化 DP 始终稳定在 1.2 MB
- **正确性验证**：用暴力穷举验证优化 DP 的正确性，结果完全一致

---

## 六、总结（1 分钟）

回顾一下我们的工作：

1. **问题建模**：将电商促销选品问题形式化为二维约束的 0-1 背包问题，并引入多目标权重调节
2. **算法设计**：实现了三种算法——优化 DP（主推）、贪心（对比）、暴力回溯（验证）
3. **空间优化**：通过分治回溯将 DP 空间从 O(N×W×B) 降至 O(W×B)
4. **完整验证**：用暴力穷举证明 DP 正确性，用性能曲线展示时间复杂度差异，用对比表格展示空间复杂度差异

我们的系统不仅是一个算法实现，更是一个**可交互的决策工具**——用户可以实时调节预算、权重、商品数据，观察不同算法在不同场景下的表现。

谢谢各位老师，欢迎提问。

---

## 预备：可能的答辩问题

**Q1：为什么不用归一化处理两个不同量纲的指标？**
> A：在当前数据范围下（喜爱度 30-100，佣金 20-120），两个指标的数值范围接近，归一化不会显著改变排序结果。但系统设计上已预留了扩展空间，若需处理更大范围差异，可加入 Min-Max 归一化。

**Q2：贪心在什么情况下会等于最优解？**
> A：当商品的"单位成本价值比"排序恰好等于全局最优的选取顺序时，贪心就能达到最优。这通常发生在商品性价比差异明显、没有"互补效应"的场景下。

**Q3：暴力回溯在小规模下反而比 DP 快，为什么？**
> A：DP 的瓶颈不在 N，而在预算维度。当 N 小但预算大时，DP 需要填充一张很大的二维表格，而暴力只需枚举 2^N 种组合。但随着 N 增大，暴力的指数增长会迅速崩溃——这就是多项式时间和指数时间的本质差异。

**Q4：为什么主推优化 DP 而不是标准 DP？**
> A：两者结果完全一致，但优化 DP 的空间从 O(N×W×B) 降至 O(W×B)。在算法设计中，主动优化空间复杂度是更成熟的工程实践。虽然当前规模下标准 DP 也够用，但优化版可以无缝应对更大规模的数据。

**Q5：分治回溯的时间为什么比标准 DP 慢约 2 倍？**
> A：分治回溯在每一层递归中都要跑一次正向 DP 和一次反向 DP，相当于把标准 DP 的工作做了两遍。但递归深度只有 log₂N 层，所以总时间约为标准 DP 的 2 倍，而不是 N 倍。
